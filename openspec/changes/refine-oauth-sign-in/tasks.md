## 1. Google Cloud setup (external, no code)

- [x] 1.1 Create an OAuth 2.0 Client ID in Google Cloud Console for this app, requesting only the `email`, `profile`, and `openid` scopes, and verify the client ID/secret are generated.
- [ ] 1.2 Publish the OAuth consent screen to **Production** (per design.md D6 — not Testing, which would require every family member to be added as a Google Cloud test user) and verify the consent screen's publishing status shows "In production." — **deferred by owner (client still in Testing)**.
- [x] 1.3 Set `GOOGLE_OAUTH_CLIENT_ID` and `GOOGLE_OAUTH_CLIENT_SECRET` as env vars on the Railway PocketBase service only (never committed, never set in local `mise.local.toml` — local dev is meant to stay OTP-only) and verify they appear in Railway's service variables.

## 2. PocketBase: Google provider config

- [x] 2.1 Add a new `pb_migrations` file enabling the Google OAuth2 provider on the `users` collection, reading `GOOGLE_OAUTH_CLIENT_ID`/`GOOGLE_OAUTH_CLIENT_SECRET` via `$os.getenv(...)` and no-op'ing (provider left disabled) when either is unset — mirror `1788600004_configure_smtp.js`'s shape exactly (design.md D4). Verify: running `mise run pb:serve` locally (no env vars set) shows the `users` collection's OAuth2 settings with Google still disabled.
- [x] 2.2 Add the migration's `down()` disabling the provider again. Verify: running the migration down locally reverts the collection to its prior state with no errors.
- [x] 2.3 With the Production client ID/secret exported temporarily in a local shell, verify the same migration enables Google correctly (admin UI shows the provider configured) — then unset them again so local dev defaults back to OTP-only. — **verified against the deployed instance** instead of local (`/api/collections/users/auth-methods` → `oauth2.enabled=true, providers=["google"]`, OTP still enabled); creds were never set locally, so local dev remains OTP-only.
- [x] 2.4 Configure PocketBase's `trustedProxy` to trust `X-Real-IP` via a migration, required so the OAuth2 realtime `@oauth2` subscription is accepted behind Railway (design.md D7) — without it the subscription is `400 Invalid realtime client.` and Google sign-in cannot complete. `X-Forwarded-For` was tried first but its right-most entry varies per edge (intermittent 400s), so `X-Real-IP` (a single stable client IP) is used instead. Verify: `POST /api/realtime` with `@oauth2` returns `204`, not `400` — verified 6/6 runs.

## 3. PocketBase: fail-closed guard hook

- [x] 3.1 Add a new `pb_hooks` file registering `onRecordAuthWithOAuth2Request`, scoped to the `users` collection (design.md D1/D2): if `e.record` is `null`, throw a PocketBase `ForbiddenError` (an `ApiError`, not a plain `Error`) and do not call `e.next()`; if `e.record` is set, call `e.next()`. The handler must **not** be `async` — JSVM ignores thrown `ApiError`s from async handlers (pocketbase/pocketbase#6476). Verify: the hook file loads without error under `mise run pb:serve --dev`.
- [x] 3.2 Add a log line in the rejection branch recording the attempted email and timestamp (design.md D2). Verify: triggering the rejection path (task 3.3) shows the log line in the `pb:serve` console output. — **verified in production** via `railway logs`: `[oauth2-guard] refused unapproved sign-in nhudak3@gmail.com 2026-09-12T18:50:31.747Z`.
- [x] 3.3 **Manual verification of the fail-closed path** (no automated backend test framework exists in this repo yet — see design.md's Risks/Trade-offs for why this is a documented procedure rather than an automated test): with the Production Google client wired up, attempt to sign in via Google using an email address with **no** matching `users` record. Verify: sign-in is refused with the hook's distinct error, and the `users` record count is unchanged before and after the attempt (check via `mise run pb:admin -- GET /collections/users/records`). — **verified in production**: the Google round trip completed (`GET /api/oauth2-redirect 307` → `POST /api/collections/users/auth-with-oauth2 403`), the UI showed the refusal copy, and `users` count was `0` before and after (no record created).
- [x] 3.4 **Manual verification of the matched path**: create a `users` record for a test email (admin action), then sign in via Google using that same email. Verify: sign-in succeeds, exactly one `users` record exists for that email (no duplicate created), and the record is now linked to the Google identity. — **verified in production**: sign-in succeeded, `users` total = 1 (no duplicate), and `_externalAuths` has one `google` link (`recordRef = c8bqqlvm3htx1oe`).

## 4. Frontend: Google sign-in

- [x] 4.1 Add `signInWithGoogle()` to `AuthContext.tsx`, calling `pb.collection('users').authWithOAuth2({ provider: 'google' })`, alongside the existing `requestOtp`/`confirmOtp` (design.md D5). Verify: `pnpm build` (tsc) passes with no type errors.
- [x] 4.2 Catch the guard hook's refusal in `signInWithGoogle()` (or its caller) by matching `err.status === 403` — not a string match (design.md D5) — separately from other failures, and surface a specific "not approved" state to the UI rather than a generic error. Verify: before task 4.3 ships, a manual `authWithOAuth2` call with an unapproved Google account confirms the `403` and its message actually reach the client through the popup flow; then a component-level check (manual or test) confirms the refusal is distinguishable from a network-error state (`status === 0`) and other HTTP failures. — **verified in production**: the unapproved account produced the specific refusal message, while earlier non-403 failures (realtime 400) produced the generic "Could not sign in with Google" message — so the two states are distinguishable in practice.
- [x] 4.3 Add a Google sign-in button to `SignInForm.tsx` alongside the existing email-code form, and use the settled refusal copy for the "not approved" state: **"That address isn't on the list yet - contact Nich for help."** (design.md D5). Verify: `pnpm lint` and `pnpm test` pass; a manual click-through in `pnpm dev` shows both sign-in options and the refusal message renders correctly for an unapproved test account. — **verified in production** (deployed bundle contains both the button and the copy; the click-through showed exactly that message). `pnpm lint`/`pnpm test` pass locally.

## 5. Rollout

- [x] 5.1 Land the migration (task 2.1) and the guard hook (task 3.1) on `main` together — landed in the same commit (`5817e5c`) and pushed directly to `main` (this repo has no PR workflow; the owner chose a direct deploy). `deploy-pocketbase.yml`'s CI deploys everything under `pocketbase/**` together (Dockerfile `COPY`s both directories; no workflow change needed, but `mise.toml`'s `pb:deploy` needed `--path-as-root` added — discovered during implementation, since CI calls that task). Verified: both Actions runs succeeded; Railway deployment `06ec9bcd` is SUCCESS; `railway ssh` shows `/pb/pb_hooks/guard_oauth2_signin.pb.js` present in the image; startup logs show no JSVM error; `/api/collections/users/auth-methods` shows `oauth2.enabled=true, providers=["google"], otp.enabled=true`.
- [x] 5.2 End-to-end regression check against the deployed environment: an existing account holder still signs in via the one-time email code exactly as before (identity spec's unchanged "Returning account holder signs in" scenario). Verify: sign-in via OTP succeeds with no observable change in behavior. — **verified in production**: OTP email received and sign-in succeeded for `nhudak3@gmail.com`.
- [x] 5.3 End-to-end check against the deployed environment: an approved account holder signs in via Google for the first time and lands signed in (identity spec's new "Returning account holder signs in with a federated provider" scenario). Verify: sign-in succeeds and the header shows the signed-in state. — **verified in production**: Google sign-in landed signed in.
